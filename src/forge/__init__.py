"""forge — data masking and synthetic generation CLI.

The CLI is a thin terminal wrapper around forge_engine. All data logic
lives in forge_engine; this package only handles command parsing,
terminal UX, and forwarding work to the engine.
"""

__version__ = "0.1.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "forge"
version = "0.1.0"
description = "Forge CLI — data masking and synthetic generation from the terminal"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "louiskeep" }]
dependencies = [
    "forge-engine>=0.1.0",
    "typer>=0.9",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[project.scripts]
forge = "forge.__main__:app"

[tool.hatch.build.targets.wheel]
packages = ["src/forge"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
